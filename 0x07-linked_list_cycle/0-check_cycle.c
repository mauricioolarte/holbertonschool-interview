#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"


/**
 * check_cycle - list
 * @list: linked list
 * Return: int
 * Description: singly linked list node structure
 *
 */
int check_cycle(listint_t *list)
{
	if (list == NULL)
	return (NULL);
}
